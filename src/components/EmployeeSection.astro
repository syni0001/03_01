---
const {data}= Astro.props;
import EmployeeCard from "../components/EmployeeCard.astro"

---
<section>
    <h1>{data.title}</h1>
    {
        data.values.map(employee => <EmployeeCard {...employee} />)
    }

<!-- {data.map((employee) => <EmployeeCard {...employee}/>)} -->
<!-- {data.map((employee) => (<EmployeeCard />))} -->






</section>


